<% layout("/layout") -%>
<p>Posts list:</p>
<% if(posts.length > 0 ){%>
  <ul>
    <% posts.forEach(function(i, e){ %>
      <li><%- i.title %></li>
    <% }) %>
  </ul>
<% }else { %>
    <p>No posts available</p>
<% } %>
<%- partial('form') %>
